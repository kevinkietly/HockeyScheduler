package model;

public class Goalie extends Player {
    public Goalie(int id, String name, int number, String plays_in_since, int teamID) {
        super(id, name, number, plays_in_since, teamID);
    }
}
