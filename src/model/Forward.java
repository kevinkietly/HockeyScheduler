package model;

public class Forward extends Player{
    public Forward(int id, String name, int number, String plays_in_since, int teamID) {
        super(id, name, number, plays_in_since, teamID);
    }
}
