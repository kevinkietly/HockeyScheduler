package model;

public class Forward extends player {
    public Forward(int player_id, String name, int number, String plays_in_since, int team_id) {
        super(player_id, name, number, plays_in_since, team_id);
    }
}
