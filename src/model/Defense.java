package model;

public class Defense extends Player{
    public Defense(int id, String name, int number, String plays_in_since, int teamID) {
        super(id, name, number, plays_in_since, teamID);
    }
}
